# Sideline Manager (UI polish update)

This build keeps the dynamic fairness scheduler and adds visual polish:
- removed the CSV schedule button
- stronger title styling so it stands out above the stripe theme
- bigger icon
- smaller right-aligned back-to-landing button
- green grass decorative strip under the Sideline Manager title

It also keeps the dynamic split / fairness engine, injury resets, and the landing page.
