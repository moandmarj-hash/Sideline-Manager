# Sideline Manager (banner style + mobile layout fix)

This build tightens the selected banner styling to better match the chosen concept:
- icon sits in a translucent outer shell with a red inner tile
- title stays to the right of the icon on mobile
- grass strip and tagline sit under the title without stacking into a giant header
- current block timer layout is consolidated into a left information column and right button column on mobile
