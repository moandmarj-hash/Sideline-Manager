# Sideline Manager (banner + icon + contrast update)

This build updates the app with:
- the rounded-square clipboard/interchange icon as the main app icon and browser-tab favicon
- the new banner styling using the selected icon and the SIDELINE MANAGER title treatment
- the app stripe colour updated to the warmer club red (#E0464B)
- stronger contrast in the current block by making the go-on and come-off name containers solid green and solid red with white text

If the browser tab icon does not change right away after deploy, clear the browser cache once or hard refresh the site.
