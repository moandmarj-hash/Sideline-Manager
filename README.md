# Sideline Manager (cross-squad quick swap update)

This build adds squad selectors to both sides of the Quick Swap tool so a player can come off from one squad and be replaced by a player from the other squad when needed.

The scheduling logic is updated so the current block override can change the White / Red split for that block, and the projected minutes / future rotation plan rebalance from there.
